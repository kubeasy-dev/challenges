apiVersion: challenge.kubeasy.dev/v1alpha1
kind: StatusValidation
metadata:
  name: pod-ready-check
spec:
  target:
    kind: Pod
    labelSelector:
      matchLabels:
        app: config-app
  expectedConditions:
    - type: "Ready"
      status: "True"
