apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    app: front-end  # BUG: should be "frontend" (no hyphen)
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
