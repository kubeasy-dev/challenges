apiVersion: v1
kind: Service
metadata:
  name: api-service
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    app: api-service  # Correct selector matching deployment
    version: v1
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
