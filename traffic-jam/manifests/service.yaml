apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  selector:
    app: api-backend
    version: v1
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP