apiVersion: challenge.kubeasy.dev/v1alpha1
kind: StatusValidation
metadata:
  name: pods-ready-check
spec:
  target:
    kind: Pod
    labelSelector:
      matchLabels:
        app: api
  expectedConditions:
    - type: "Ready"
      status: "True"
