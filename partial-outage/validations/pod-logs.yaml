apiVersion: challenge.kubeasy.dev/v1alpha1
kind: LogValidation
metadata:
  name: frontend-logs-check
spec:
  target:
    apiVersion: v1
    kind: Pod
    labelSelector:
      matchLabels:
        app: frontend
  expectedStrings:
    - "✅ Connected to backend"
  sinceSeconds: 120
