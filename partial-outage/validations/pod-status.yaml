apiVersion: challenge.kubeasy.dev/v1alpha1
kind: StatusValidation
metadata:
  name: frontend-ready-check
spec:
  target:
    kind: Pod
    labelSelector:
      matchLabels:
        app: frontend
  expectedConditions:
    - type: "Ready"
      status: "True"
