apiVersion: challenge.kubeasy.dev/v1alpha1
kind: StatusValidation
metadata:
  name: app-ready-check
spec:
  target:
    kind: Pod
    labelSelector:
      matchLabels:
        app: probes-app
  expectedConditions:
    - type: "Ready"
      status: "True"
