apiVersion: challenge.kubeasy.dev/v1alpha1
kind: StatusValidation
metadata:
  name: pod-ready-check
spec:
  targetRef:
    kind: Pod
    labelSelector:
      matchLabels:
        app: legacy-app
  expectedConditions:
    - type: "Ready"
      status: "True"
