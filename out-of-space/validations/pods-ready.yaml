apiVersion: challenge.kubeasy.dev/v1alpha1
kind: StatusValidation
metadata:
  name: all-postgres-pods-ready
spec:
  target:
    kind: Pod
    labelSelector:
      matchLabels:
        app: postgres
  expectedConditions:
    - type: "Ready"
      status: "True"
